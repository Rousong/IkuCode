# AGENTS.md

如果修改此项目里面的代码文件，那么请同步更新此 MD 文档。

<!-- USER-RULES:START -->
## 当前项目是一个使用 uv 管理的 Python 项目

**始终使用当前目录的 `.venv`，并以 `pyproject.toml` 与 `uv.lock` 作为依赖事实来源。**

- 处理 Python 项目时，优先使用当前目录下由 `uv` 管理的 `.venv` 虚拟环境。
- 如果当前目录还没有 `.venv`，先使用 `uv venv` 创建，再进行安装依赖、运行脚本、执行测试等操作。
- 安装、删除或调整依赖时，优先使用 `uv add`、`uv remove` 等 `uv` 命令，不要手动改写已安装环境来绕过依赖声明。
- 依赖声明以 `pyproject.toml` 为准，锁定结果以 `uv.lock` 为准；只要依赖发生变化，就要同步维护这两个文件。
- 执行项目命令时，优先使用 `uv run`；需要让本地环境与锁文件对齐时，使用 `uv sync`。
- 除非项目明确要求导出兼容文件，否则不要把 `requirements.txt` 作为主依赖清单，也不要把它当成环境真实状态的唯一来源。

## Python 编码与文档规范

这是一套偏严格的项目规范，适用于长期维护或需要多人 / 多 Agent 协作的 Python 自动化项目。一次性小脚本、测试辅助代码或探索性脚本，可以在不影响可读性和可维护性的前提下适当简化样板。

- 新增或修改 Python 模块时，文件头必须使用以下格式。`File:` 不要写死具体文件名，应替换为当前模块文件名：

```python
#!/usr/bin/env python
# -*- coding: utf-8 -*-

"""
File: 当前模块文件名.py
Author:
Date:
Version: 1.0.0
Description:
"""
```

- 新增方法时，必须在方法定义外部紧邻上方添加如下格式的方法标识注释。这里不要写死固定人名，应替换为当前方法名或处理主题：

```python
# ------------------------------------------------------------
# 在这里填写方法名或处理主题
# ------------------------------------------------------------
```

- 每个函数或方法内部都必须编写 docstring，并至少包含方法简介、`Args:` 和 `Returns:`，推荐使用以下格式：

```python
"""
方法的简介

Args:

Returns:
"""
```

- 所有函数和方法都必须显式声明参数类型和返回值类型。
- 如果参数较多、返回值结构较复杂，必须在 docstring 中补充说明各参数含义、约束条件、默认值以及返回结果的结构。
<!-- USER-RULES:END -->

## 代码简介

这是一个使用 Python 和 DeepSeek API 的终端对话程序，目标是以尽量少的代码提供类似 Claude Code 的基本交互体验，支持多轮对话、本地 tool use，以及通过 `.env` 管理本地 API 配置。

## 目录结构

```text
IkuCode/ # 项目根目录，存放整个终端对话程序及说明文档。
├── AGENTS.md # 项目级协作说明文档，记录约束、代码简介和目录结构。
├── .env # 本地环境变量文件，保存 DeepSeek API Key 和模型配置，不提交到仓库。
├── .gitignore # Git 忽略规则，当前用于忽略本地 .env 文件。
├── README.md # 项目使用说明，包含环境变量、uv 命令和交互方式。
├── claude_code_like.py # 主模块，仅保留系统提示词和 main 入口。
├── methods.py # 对话流程与模型调用模块，负责 .env 加载、会话循环和 tool call 编排。
└── pyproject.toml # uv / Python 工程配置，声明依赖和命令入口。
└── tools.py # 本地工具定义模块，负责工具函数、路径约束和工具元数据。
```
